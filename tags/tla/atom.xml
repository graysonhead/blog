<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
    <title> - TLA</title>
    <link href="https://blog.graysonhead.net/tags/tla/atom.xml" rel="self" type="application/atom+xml"/>
    <link href="https://blog.graysonhead.net"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>2026-05-02T00:00:00+00:00</updated>
    <id>https://blog.graysonhead.net/tags/tla/atom.xml</id>
    <entry xml:lang="en">
        <title>How to Know Your Distributed System is Correct (Before It Exists)</title>
        <published>2026-05-02T00:00:00+00:00</published>
        <updated>2026-05-02T00:00:00+00:00</updated>
        <author>
          <name>Unknown</name>
        </author>
        <link rel="alternate" href="https://blog.graysonhead.net/posts/tla-plus/" type="text/html"/>
        <id>https://blog.graysonhead.net/posts/tla-plus/</id>
        
        <content type="html">&lt;p&gt;Wouldn&#x27;t it be neat if you could, before writing a single line of code, exhaustively test your distributed system, network protocol, or state machine?&lt;&#x2F;p&gt;
&lt;p&gt;For the low, low cost of learning a (very) obscure language, you can!&lt;&#x2F;p&gt;
&lt;h1 id=&quot;planning-in-an-action-oriented-era&quot;&gt;Planning in an Action Oriented Era&lt;&#x2F;h1&gt;
&lt;p&gt;Modern companies seem biased to an extreme degree on leaping before you look. It has been my personal experience, and the experience of many of my colleagues, that most tech companies would rather you start by writing a prototype and bouncing around the problem space until you run into the right solution. I call this the Roomba design process.&lt;&#x2F;p&gt;
&lt;p&gt;I wasn&#x27;t around for the &amp;quot;good old days&amp;quot; of software development, but after hearing mentor after mentor pine for the days when one actually sat down and wrote a plan for a software development project, it made me wonder if perhaps we left a good thing behind in the name of shipping an MVP quicker.&lt;&#x2F;p&gt;
&lt;p&gt;Is a heavily planned approach actually better? Or do all these tech companies have it right at the end of the day? After all, the introduction of generative AI into the software development process has significantly increased the velocity of the Roomba.&lt;&#x2F;p&gt;
&lt;p&gt;I haven&#x27;t talked much about my AI assisted software development strategy on this blog yet, but an aspect of my methodology that is very relevant to this topic is that applying the same constraints to AI models that I would impose upon a human developer delivers very similar results. The most relevant example of this is that all the test cases need to pass, and any new feature requires new test cases. I&#x27;d go so far as to say that deciding what should be tested is perhaps the part of the AI assisted software development process in which the developer should be the most engaged.&lt;&#x2F;p&gt;
&lt;p&gt;These days, when programming with AI, I like to spend a lot more time reviewing and iterating on test cases than I do on the actual application logic itself. To me, the test cases represent a contract regarding how the program should behave. And in an era where code is dynamically generated by a statistical model, this contract is more important than ever. Especially since AI can produce code faster than you can reason about its correctness. These models are just as prone to mistakes as humans (sometimes more often&lt;sup class=&quot;footnote-reference&quot;&gt;&lt;a href=&quot;#2&quot;&gt;1&lt;&#x2F;a&gt;&lt;&#x2F;sup&gt;) and often lack broader long-term contextual knowledge.&lt;&#x2F;p&gt;
&lt;p&gt;But in a distributed system, with complex behavior, it&#x27;s often difficult to reason about what should actually be in this contract in the first place. And here, keeping the broad context in mind while actually implementing your system is incredibly important.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;meet-tla&quot;&gt;Meet TLA+&lt;&#x2F;h1&gt;
&lt;p&gt;TLA+ was created by &lt;a href=&quot;https:&#x2F;&#x2F;www.lamport.org&#x2F;&quot;&gt;Leslie Lamport&lt;&#x2F;a&gt;, a computer scientist perhaps best known for inventing the Paxos consensus algorithm and (less glamorously but no less influentially) developing LaTeX. Judging from his publications I must assume that Lamport spent decades thinking rigorously about concurrency and distributed systems, and TLA+ is the distillation of that: a formal specification language grounded in mathematics and temporal logic that lets you describe exactly how a system behaves over time.&lt;&#x2F;p&gt;
&lt;p&gt;Continuing our earlier &amp;quot;tests are a contract&amp;quot; analogy: imagine TLA+ as the first stage of drafting our contract. It is the part before the lawyers get invited, where you decide what the contract&#x27;s actual purpose is, what properties it should have, and how it actually behaves. In practice, this means writing a precise mathematical model of your system, which a tool called TLC then uses to exhaustively explore every possible state it can reach, surfacing deadlocks, race conditions, and safety violations that are notoriously hard to hunt down in production. Companies like Amazon and Microsoft have used this approach to find subtle bugs in distributed protocols.&lt;&#x2F;p&gt;
&lt;p&gt;If you want to follow along, the TLA+ Toolbox (which includes TLC) is available at &lt;a href=&quot;https:&#x2F;&#x2F;lamport.azurewebsites.net&#x2F;tla&#x2F;tla.html&quot;&gt;https:&#x2F;&#x2F;lamport.azurewebsites.net&#x2F;tla&#x2F;tla.html&lt;&#x2F;a&gt;.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;losing-money-formally-bank-transfer-example&quot;&gt;Losing Money, Formally: Bank Transfer Example&lt;&#x2F;h1&gt;
&lt;p&gt;For example, let&#x27;s say we are building a system to track bank accounts.&lt;&#x2F;p&gt;
&lt;p&gt;Here is the actual spec:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;tla&quot; style=&quot;background-color:#2b303b;color:#c0c5ce;&quot; class=&quot;language-tla &quot;&gt;&lt;code class=&quot;language-tla&quot; data-lang=&quot;tla&quot;&gt;&lt;span&gt;---------------------------- MODULE bank_transfer ----------------------------
&lt;&#x2F;span&gt;&lt;span&gt;EXTENDS Naturals, TLC
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;VARIABLES a_balance, b_balance, in_flight
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;vars == &amp;lt;&amp;lt;a_balance, b_balance, in_flight&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;TypeInvariant ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_balance \in 0..100
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_balance \in 0..100
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight \in 0..100
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;\* Total money is always conserved
&lt;&#x2F;span&gt;&lt;span&gt;MoneyConserved ==
&lt;&#x2F;span&gt;&lt;span&gt;    a_balance + b_balance + in_flight = 100
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Init ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_balance = 100
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_balance = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight = 0
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Debit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_balance &amp;gt;= 10
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_balance&amp;#39; = a_balance - 10
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight&amp;#39; = in_flight + 10
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED b_balance
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Credit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight &amp;gt; 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_balance&amp;#39; = b_balance + in_flight
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight&amp;#39; = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED a_balance
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Done ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_balance = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED vars
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Next == Debit \&#x2F; Credit \&#x2F; Done
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Spec == Init &#x2F;\ [][Next]_vars
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;=============================================================================
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;And its associated config:&lt;&#x2F;p&gt;
&lt;pre style=&quot;background-color:#2b303b;color:#c0c5ce;&quot;&gt;&lt;code&gt;&lt;span&gt;SPECIFICATION Spec
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;INVARIANTS
&lt;&#x2F;span&gt;&lt;span&gt;    TypeInvariant
&lt;&#x2F;span&gt;&lt;span&gt;    MoneyConserved
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Every spec has four primary components:&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Constants and variables&lt;&#x2F;strong&gt;: the named values that parameterize the model (constants) and the state that changes over time (variables).&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;State Predicates&lt;&#x2F;strong&gt;: expressions that are true or false about a single state. Init is one, TypeInvariant is one, MoneyConserved is one. They describe what a state looks like without reference to transitions.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;Actions&lt;&#x2F;strong&gt;: expressions that relate a current state to a next state. Everything with a prime (&#x27;). Debit, Credit, etc. They describe how the system moves between states.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;Properties&lt;&#x2F;strong&gt;: temporal formulas checked over entire behaviors. Safety invariants, liveness formulas (where &lt;code&gt;A ~&amp;gt; B&lt;&#x2F;code&gt; means &amp;quot;A leads to B&amp;quot;), the Spec itself. Safety properties say &amp;quot;something bad never happens&amp;quot; (&lt;code&gt;MoneyConserved&lt;&#x2F;code&gt; is one). Liveness properties say &amp;quot;something good eventually happens&amp;quot; (like both sides of the protocol eventually finishing their transmission).&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;To go a bit deeper, let&#x27;s look at one of our predicates:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;tla+&quot; style=&quot;background-color:#2b303b;color:#c0c5ce;&quot; class=&quot;language-tla+ &quot;&gt;&lt;code class=&quot;language-tla+&quot; data-lang=&quot;tla+&quot;&gt;&lt;span&gt;\* Total money is always conserved
&lt;&#x2F;span&gt;&lt;span&gt;MoneyConserved ==
&lt;&#x2F;span&gt;&lt;span&gt;    a_balance + b_balance + in_flight = 100
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Anyone who knows any amount of programming can probably easily read this. Since our Init predicate specified the starting amount of money as 100, and we only move around money, there should always be a total value of 100 in the system at all times. And because we specify this property as an invariant in our config, it will be ensured that we do not violate this.&lt;&#x2F;p&gt;
&lt;p&gt;Now, for an action:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;tla+&quot; style=&quot;background-color:#2b303b;color:#c0c5ce;&quot; class=&quot;language-tla+ &quot;&gt;&lt;code class=&quot;language-tla+&quot; data-lang=&quot;tla+&quot;&gt;&lt;span&gt;Debit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_balance &amp;gt;= 10
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_balance&amp;#39; = a_balance - 10
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight&amp;#39; = in_flight + 10
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED b_balance
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;The first thing you will notice is these ASCII wickets (&lt;code&gt;&#x2F;\&lt;&#x2F;code&gt; and &lt;code&gt;\&#x2F;&lt;&#x2F;code&gt;). Not coming from a math background, I wasn&#x27;t sure what these were supposed to mean, but to keep it simple &lt;code&gt;&#x2F;\&lt;&#x2F;code&gt; is a logical AND and &lt;code&gt;\&#x2F;&lt;&#x2F;code&gt; is a logical OR. Any variable with a &lt;code&gt;&#x27;&lt;&#x2F;code&gt; refers to its value in the next state, and any variable without refers to its value in the current state. So, this action specifies:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;pseudocode&quot; style=&quot;background-color:#2b303b;color:#c0c5ce;&quot; class=&quot;language-pseudocode &quot;&gt;&lt;code class=&quot;language-pseudocode&quot; data-lang=&quot;pseudocode&quot;&gt;&lt;span&gt;if a_balance &amp;gt;= 10 
&lt;&#x2F;span&gt;&lt;span&gt;set a_balance = a_balance - 10
&lt;&#x2F;span&gt;&lt;span&gt;and
&lt;&#x2F;span&gt;&lt;span&gt;set in_flight = in_flight + 10
&lt;&#x2F;span&gt;&lt;span&gt;leave b_balance unchanged
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Technically, the predicates in this action (a_balance &amp;gt;= 10) are not if statements. TLC simply won&#x27;t consider this action for execution unless they are satisfied.&lt;&#x2F;p&gt;
&lt;p&gt;Now we have the Next action:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;tla&quot; style=&quot;background-color:#2b303b;color:#c0c5ce;&quot; class=&quot;language-tla &quot;&gt;&lt;code class=&quot;language-tla&quot; data-lang=&quot;tla&quot;&gt;&lt;span&gt;Next == Debit \&#x2F; Credit \&#x2F; Done
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;This is basically the menu of what actions TLC can take at any given step. Since they&#x27;re joined with &lt;code&gt;\&#x2F;&lt;&#x2F;code&gt; (or), TLC will explore all valid combinations: steps where &lt;code&gt;Debit&lt;&#x2F;code&gt; fires, steps where &lt;code&gt;Credit&lt;&#x2F;code&gt; fires, and steps where &lt;code&gt;Done&lt;&#x2F;code&gt; fires. The &lt;code&gt;Done&lt;&#x2F;code&gt; action is interesting. It&#x27;s a terminal state that kicks in once all the money has moved over (&lt;code&gt;a_balance = 0&lt;&#x2F;code&gt; and nothing is in flight). It doesn&#x27;t change anything (&lt;code&gt;UNCHANGED vars&lt;&#x2F;code&gt;), it just allows the system to idle gracefully rather than getting stuck. Without it, TLC might flag a deadlock when the transfer completes and no other actions are enabled.&lt;&#x2F;p&gt;
&lt;p&gt;And finally:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;tla&quot; style=&quot;background-color:#2b303b;color:#c0c5ce;&quot; class=&quot;language-tla &quot;&gt;&lt;code class=&quot;language-tla&quot; data-lang=&quot;tla&quot;&gt;&lt;span&gt;Spec == Init &#x2F;\ [][Next]_vars
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;This is the top-level definition that ties everything together. It says: the system starts in &lt;code&gt;Init&lt;&#x2F;code&gt; AND, in every subsequent step, either a &lt;code&gt;Next&lt;&#x2F;code&gt; action occurs or the variables stay the same. That last part, &lt;code&gt;[][Next]_vars&lt;&#x2F;code&gt;, is where the temporal logic comes in. The &lt;code&gt;[]&lt;&#x2F;code&gt; means &amp;quot;always&amp;quot; (as in, at every point in time), and the &lt;code&gt;_vars&lt;&#x2F;code&gt; subscript allows for &amp;quot;stuttering steps&amp;quot; where nothing changes. Stuttering steps are a deliberate design choice to allow specs to be composed together cleanly, but you can mostly think of it as: at every moment, the system either takes a valid step or idles.&lt;&#x2F;p&gt;
&lt;p&gt;When TLC runs, this is what it checks against. It takes your Spec, generates the full graph of reachable states, and then verifies that your invariants hold in every single one of them.&lt;&#x2F;p&gt;
&lt;p&gt;We can evaluate this spec like so:&lt;&#x2F;p&gt;
&lt;pre style=&quot;background-color:#2b303b;color:#c0c5ce;&quot;&gt;&lt;code&gt;&lt;span&gt;tlc bank_transfer.tla
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;And we will get output like this:&lt;&#x2F;p&gt;
&lt;pre style=&quot;background-color:#2b303b;color:#c0c5ce;&quot;&gt;&lt;code&gt;&lt;span&gt;TLC2 Version 2.19 of 08 August 2024 (rev: 5a47802)
&lt;&#x2F;span&gt;&lt;span&gt;Running breadth-first search Model-Checking with fp 89 and seed -6445696154581258138 with 1 worker on 16 cores with 14231MB heap and 64MB offheap memory (Linux 6.12.81 amd64, Oracle Corporation 1.8.0_472 x86_64, MSBDiskFPSet, DiskStateQueue).
&lt;&#x2F;span&gt;&lt;span&gt;Parsing file &#x2F;home&#x2F;grayson&#x2F;code&#x2F;tla&#x2F;simple&#x2F;bank_transfer.tla
&lt;&#x2F;span&gt;&lt;span&gt;Parsing file &#x2F;tmp&#x2F;Naturals.tla
&lt;&#x2F;span&gt;&lt;span&gt;Parsing file &#x2F;tmp&#x2F;TLC.tla
&lt;&#x2F;span&gt;&lt;span&gt;Parsing file &#x2F;tmp&#x2F;Sequences.tla
&lt;&#x2F;span&gt;&lt;span&gt;Parsing file &#x2F;tmp&#x2F;FiniteSets.tla
&lt;&#x2F;span&gt;&lt;span&gt;Semantic processing of module Naturals
&lt;&#x2F;span&gt;&lt;span&gt;Semantic processing of module Sequences
&lt;&#x2F;span&gt;&lt;span&gt;Semantic processing of module FiniteSets
&lt;&#x2F;span&gt;&lt;span&gt;Semantic processing of module TLC
&lt;&#x2F;span&gt;&lt;span&gt;Semantic processing of module bank_transfer
&lt;&#x2F;span&gt;&lt;span&gt;Starting... (2026-04-22 20:21:57)
&lt;&#x2F;span&gt;&lt;span&gt;Computing initial states...
&lt;&#x2F;span&gt;&lt;span&gt;Finished computing initial states: 1 distinct state generated at 2026-04-22 20:21:57.
&lt;&#x2F;span&gt;&lt;span&gt;Model checking completed. No error has been found.
&lt;&#x2F;span&gt;&lt;span&gt;  Estimates of the probability that TLC did not check all reachable states
&lt;&#x2F;span&gt;&lt;span&gt;  because two distinct states had the same fingerprint:
&lt;&#x2F;span&gt;&lt;span&gt;  calculated (optimistic):  val = 1.6E-16
&lt;&#x2F;span&gt;&lt;span&gt;112 states generated, 66 distinct states found, 0 states left on queue.
&lt;&#x2F;span&gt;&lt;span&gt;The depth of the complete state graph search is 12.
&lt;&#x2F;span&gt;&lt;span&gt;The average outdegree of the complete state graph is 1 (minimum is 0, the maximum 2 and the 95th percentile is 2).
&lt;&#x2F;span&gt;&lt;span&gt;Finished in 00s at (2026-04-22 20:21:57)
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;So, what happened?&lt;&#x2F;p&gt;
&lt;p&gt;TLC ran a breadth-first search over every possible state the system can reach, starting from &lt;code&gt;Init&lt;&#x2F;code&gt;. It found 66 distinct states across 112 total steps, verified that &lt;code&gt;MoneyConserved&lt;&#x2F;code&gt; and &lt;code&gt;TypeInvariant&lt;&#x2F;code&gt; held in all of them, and reported no errors. The whole thing finished in under a second.&lt;&#x2F;p&gt;
&lt;p&gt;A few details worth noting from the output:&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;112 states generated, 66 distinct&lt;&#x2F;strong&gt; - TLC explores paths, not just states. The same state can be reached multiple ways (e.g., Debit then Debit then Credit lands in the same place as Debit then Credit then Debit), so the raw count is higher than the number of unique states.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;depth of 12&lt;&#x2F;strong&gt; - the longest possible sequence of actions from start to finish is 12 steps. Since we start with 100 and debit in increments of 10, this makes sense: 10 debits + some credits + Done.&lt;&#x2F;li&gt;
&lt;li&gt;&lt;strong&gt;max outdegree of 2&lt;&#x2F;strong&gt; - at any given state, there are at most 2 non-stuttering actions available (Debit and Credit can both be enabled at the same time). When the system has finished, no new transitions are possible, giving an outdegree of 0.&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;The key line is &lt;code&gt;Model checking completed. No error has been found.&lt;&#x2F;code&gt; That means our invariants held across every reachable state: the money was always conserved, and the balances never left their defined bounds. For a distributed system, that kind of exhaustive guarantee is something no amount of unit testing can give you.&lt;&#x2F;p&gt;
&lt;p&gt;But you might be wondering two things:&lt;&#x2F;p&gt;
&lt;ul&gt;
&lt;li&gt;This system doesn&#x27;t seem very distributed&lt;&#x2F;li&gt;
&lt;li&gt;TLA assumed everything worked perfectly and no failures happened&lt;&#x2F;li&gt;
&lt;&#x2F;ul&gt;
&lt;p&gt;And both of these are true. The first is true just because I needed a simple example that fit entirely on your screen (patience, we are getting there.). The second is something we can address now, so how do we simulate bad stuff happening? With bad actions:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;tla+&quot; style=&quot;background-color:#2b303b;color:#c0c5ce;&quot; class=&quot;language-tla+ &quot;&gt;&lt;code class=&quot;language-tla+&quot; data-lang=&quot;tla+&quot;&gt;&lt;span&gt;Crash ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight &amp;gt; 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight&amp;#39; = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_balance, b_balance&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Next == Debit \&#x2F; Credit \&#x2F; Crash
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Now, if we run TLC again with this example:&lt;&#x2F;p&gt;
&lt;pre style=&quot;background-color:#2b303b;color:#c0c5ce;&quot;&gt;&lt;code&gt;&lt;span&gt;# tlc bank_transfer.tla 
&lt;&#x2F;span&gt;&lt;span&gt;TLC2 Version 2.19 of 08 August 2024 (rev: 5a47802)
&lt;&#x2F;span&gt;&lt;span&gt;Running breadth-first search Model-Checking with fp 30 and seed -1146706225358764036 with 1 worker on 16 cores with 14231MB heap and 64MB offheap memory (Linux 6.12.81 amd64, Oracle Corporation 1.8.0_472 x86_64, MSBDiskFPSet, DiskStateQueue).
&lt;&#x2F;span&gt;&lt;span&gt;Parsing file &#x2F;home&#x2F;grayson&#x2F;code&#x2F;tla&#x2F;simple&#x2F;bank_transfer.tla
&lt;&#x2F;span&gt;&lt;span&gt;Parsing file &#x2F;tmp&#x2F;Naturals.tla
&lt;&#x2F;span&gt;&lt;span&gt;Parsing file &#x2F;tmp&#x2F;TLC.tla
&lt;&#x2F;span&gt;&lt;span&gt;Parsing file &#x2F;tmp&#x2F;Sequences.tla
&lt;&#x2F;span&gt;&lt;span&gt;Parsing file &#x2F;tmp&#x2F;FiniteSets.tla
&lt;&#x2F;span&gt;&lt;span&gt;Semantic processing of module Naturals
&lt;&#x2F;span&gt;&lt;span&gt;Semantic processing of module Sequences
&lt;&#x2F;span&gt;&lt;span&gt;Semantic processing of module FiniteSets
&lt;&#x2F;span&gt;&lt;span&gt;Semantic processing of module TLC
&lt;&#x2F;span&gt;&lt;span&gt;Semantic processing of module bank_transfer
&lt;&#x2F;span&gt;&lt;span&gt;Starting... (2026-04-22 20:32:20)
&lt;&#x2F;span&gt;&lt;span&gt;Computing initial states...
&lt;&#x2F;span&gt;&lt;span&gt;Finished computing initial states: 1 distinct state generated at 2026-04-22 20:32:20.
&lt;&#x2F;span&gt;&lt;span&gt;Error: Invariant MoneyConserved is violated.
&lt;&#x2F;span&gt;&lt;span&gt;Error: The behavior up to this point is:
&lt;&#x2F;span&gt;&lt;span&gt;State 1: &amp;lt;Initial predicate&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;&#x2F;\ a_balance = 100
&lt;&#x2F;span&gt;&lt;span&gt;&#x2F;\ in_flight = 0
&lt;&#x2F;span&gt;&lt;span&gt;&#x2F;\ b_balance = 0
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;State 2: &amp;lt;Debit line 23, col 5 to line 26, col 26 of module bank_transfer&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;&#x2F;\ a_balance = 90
&lt;&#x2F;span&gt;&lt;span&gt;&#x2F;\ in_flight = 10
&lt;&#x2F;span&gt;&lt;span&gt;&#x2F;\ b_balance = 0
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;State 3: &amp;lt;Crash line 40, col 5 to line 42, col 41 of module bank_transfer&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;&#x2F;\ a_balance = 90
&lt;&#x2F;span&gt;&lt;span&gt;&#x2F;\ in_flight = 0
&lt;&#x2F;span&gt;&lt;span&gt;&#x2F;\ b_balance = 0
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;5 states generated, 5 distinct states found, 2 states left on queue.
&lt;&#x2F;span&gt;&lt;span&gt;The depth of the complete state graph search is 3.
&lt;&#x2F;span&gt;&lt;span&gt;The average outdegree of the complete state graph is 1 (minimum is 1, the maximum 1 and the 95th percentile is 1).
&lt;&#x2F;span&gt;&lt;span&gt;Finished in 00s at (2026-04-22 20:32:20)
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;We get a replayed version of the chain of states that led to our &lt;code&gt;MoneyConserved&lt;&#x2F;code&gt; property being violated. First there was the initial state, then there was a Debit, and then a crash occurred.&lt;&#x2F;p&gt;
&lt;p&gt;And this lets us think, in an abstract yet very real way, about how we would solve this problem in our program. For example, if we wanted exactly-once processing of a transaction, we could implement a transaction log that is synchronously written to before the transaction takes place:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;tla+&quot; style=&quot;background-color:#2b303b;color:#c0c5ce;&quot; class=&quot;language-tla+ &quot;&gt;&lt;code class=&quot;language-tla+&quot; data-lang=&quot;tla+&quot;&gt;&lt;span&gt;\* log is authoritative: in_flight is ephemeral and may be lost on crash
&lt;&#x2F;span&gt;&lt;span&gt;MoneyConserved ==
&lt;&#x2F;span&gt;&lt;span&gt;    a_balance + b_balance + log = 100
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Init ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_balance = 100
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_balance = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ log = 0                \* added: no pending transfer at start
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Debit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ log = 0                \* added: block new transfer while one is pending
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_balance &amp;gt;= 10
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_balance&amp;#39; = a_balance - 10
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight&amp;#39; = in_flight + 10
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ log&amp;#39; = log + 10        \* added: record intent durably before moving money
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED b_balance
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Credit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight &amp;gt; 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_balance&amp;#39; = b_balance + in_flight
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight&amp;#39; = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ log&amp;#39; = 0               \* added: commit point — clear log after credit lands
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED a_balance
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Done ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_balance = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED vars
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Crash ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight &amp;gt; 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight&amp;#39; = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_balance, b_balance, log&amp;gt;&amp;gt;  \* added: log persists across crash
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;\* added: re-populate in_flight from log so Credit can retry after a crash
&lt;&#x2F;span&gt;&lt;span&gt;Recover ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ log &amp;gt; 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ in_flight&amp;#39; = log
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_balance, b_balance, log&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Next == Debit \&#x2F; Credit \&#x2F; Crash \&#x2F; Recover \&#x2F; Done
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;It is worth reiterating, since this is what makes TLA such an effective tool: TLA+ is not randomly deciding when to crash the system. TLC will exercise the crash action in every single state where it is enabled. Whenever &lt;code&gt;in_flight &amp;gt; 0&lt;&#x2F;code&gt;, TLC will explore both the branch where &lt;code&gt;Credit&lt;&#x2F;code&gt; fires and the branch where &lt;code&gt;Crash&lt;&#x2F;code&gt; fires. There is no probability involved, no random sampling, no hope that your chaos monkey hit the right moment. If a crash can happen at a given point in the execution, TLC guarantees it &lt;em&gt;will&lt;&#x2F;em&gt; happen in the model. The spec above verified correctly because it survived every one of those branches.&lt;&#x2F;p&gt;
&lt;h1 id=&quot;network-protocol-example&quot;&gt;Network Protocol Example&lt;&#x2F;h1&gt;
&lt;p&gt;Network protocols tend to have pretty nasty bugs related to state deadlocks, and TLA+ is great at finding these. Let&#x27;s work through an example.&lt;&#x2F;p&gt;
&lt;p&gt;This one comes from a potential future project. This example is modeled off of a program that uses LoRa radios to communicate. LoRa is inherently half-duplex, since any node can only be transmitting or listening at one time. So if we want to build a bidirectional protocol, we need to consider this. Fortunately, LoRa does support a limited form of carrier sense&lt;sup class=&quot;footnote-reference&quot;&gt;&lt;a href=&quot;#1&quot;&gt;2&lt;&#x2F;a&gt;&lt;&#x2F;sup&gt;, so we do have a &amp;quot;magic&amp;quot; method of determining if the other side is talking and we don&#x27;t have to rely exclusively on timing as a result. But what we are effectively building is a &lt;a href=&quot;https:&#x2F;&#x2F;en.wikipedia.org&#x2F;wiki&#x2F;Time-division_multiplexing&quot;&gt;Time Division Multiplexing&lt;&#x2F;a&gt; protocol.&lt;&#x2F;p&gt;
&lt;p&gt;So, let&#x27;s think about what kind of properties are important to this protocol. Let&#x27;s imagine that each side has frames that they both want to transmit. First, we don&#x27;t want the two sides to transmit at the same time, that would be wasteful. We can call that Safety Property &lt;code&gt;NoSimultaneousTransmit&lt;&#x2F;code&gt;. Next, we want a liveness property that specifies that each side can send all of its frames eventually, which we can call &lt;code&gt;BothEventuallyDone&lt;&#x2F;code&gt;. And finally, we want a safety invariant ensuring neither side monopolizes the channel, which we can call &lt;code&gt;BothGetATurn&lt;&#x2F;code&gt;: if A finishes sending all its frames, B must have transmitted at least once, and vice versa.&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;tla+&quot; style=&quot;background-color:#2b303b;color:#c0c5ce;&quot; class=&quot;language-tla+ &quot;&gt;&lt;code class=&quot;language-tla+&quot; data-lang=&quot;tla+&quot;&gt;&lt;span&gt;---------------------------- MODULE DuplexTDM ----------------------------
&lt;&#x2F;span&gt;&lt;span&gt;EXTENDS Naturals, TLC
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;(* Bidirectional half-duplex radio protocol.                              *)
&lt;&#x2F;span&gt;&lt;span&gt;(*                                                                        *)
&lt;&#x2F;span&gt;&lt;span&gt;(* Two nodes share a single frequency. Either node may transmit, but     *)
&lt;&#x2F;span&gt;&lt;span&gt;(* only one at a time. The transmitting node advertises a window size     *)
&lt;&#x2F;span&gt;&lt;span&gt;(* before sending. The receiver waits slightly longer than the expected   *)
&lt;&#x2F;span&gt;&lt;span&gt;(* transmission duration, then ACKs received frames and NACKs gaps.      *)
&lt;&#x2F;span&gt;&lt;span&gt;(*                                                                        *)
&lt;&#x2F;span&gt;&lt;span&gt;(* Both nodes have data to send. They must take turns fairly.            *)
&lt;&#x2F;span&gt;&lt;span&gt;(*                                                                        *)
&lt;&#x2F;span&gt;&lt;span&gt;(* Safety properties:                                                     *)
&lt;&#x2F;span&gt;&lt;span&gt;(*   - Both nodes never transmit simultaneously                           *)
&lt;&#x2F;span&gt;&lt;span&gt;(*   - ACK&#x2F;NACK never sent while other side is transmitting               *)
&lt;&#x2F;span&gt;&lt;span&gt;(*   - Delivered sequence numbers are monotonically increasing            *)
&lt;&#x2F;span&gt;&lt;span&gt;(*                                                                        *)
&lt;&#x2F;span&gt;&lt;span&gt;(* Liveness properties:                                                   *)
&lt;&#x2F;span&gt;&lt;span&gt;(*   - Neither side is starved -- if a node has data, it eventually gets  *)
&lt;&#x2F;span&gt;&lt;span&gt;(*     a turn                                                             *)
&lt;&#x2F;span&gt;&lt;span&gt;(*   - A NACK eventually triggers retransmission                          *)
&lt;&#x2F;span&gt;&lt;span&gt;(*   - Data is eventually delivered despite loss                          *)
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;CONSTANTS
&lt;&#x2F;span&gt;&lt;span&gt;    MAX_WINDOW,       \* Maximum frames a node may transmit in one turn
&lt;&#x2F;span&gt;&lt;span&gt;    MAX_SEQ,          \* Maximum sequence number (wraps, but kept simple here)
&lt;&#x2F;span&gt;&lt;span&gt;    MAX_SENT          \* Maximum frames each side can send
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;(* Node states                                                            *)
&lt;&#x2F;span&gt;&lt;span&gt;(*                                                                        *)
&lt;&#x2F;span&gt;&lt;span&gt;(* Both nodes are symmetric -- each can be in any of these states.       *)
&lt;&#x2F;span&gt;&lt;span&gt;(*                                                                        *)
&lt;&#x2F;span&gt;&lt;span&gt;(* &amp;quot;listening&amp;quot;    -- monitoring the channel; will transmit when carrier  *)
&lt;&#x2F;span&gt;&lt;span&gt;(*                   goes idle (carrier busy = receiving frames)         *)
&lt;&#x2F;span&gt;&lt;span&gt;(* &amp;quot;transmitting&amp;quot; -- actively sending frames, carrier is busy            *)
&lt;&#x2F;span&gt;&lt;span&gt;(* &amp;quot;sending_ack&amp;quot;  -- carrier just went idle, firing ACK or NACK         *)
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;NodeStates == {&amp;quot;listening&amp;quot;, &amp;quot;transmitting&amp;quot;}
&lt;&#x2F;span&gt;&lt;span&gt;NodeTxType == {&amp;quot;data&amp;quot;, &amp;quot;ack&amp;quot;, &amp;quot;nack&amp;quot;}
&lt;&#x2F;span&gt;&lt;span&gt;VARIABLES
&lt;&#x2F;span&gt;&lt;span&gt;    \* Per-node state
&lt;&#x2F;span&gt;&lt;span&gt;    a_state,          \* current state of node A
&lt;&#x2F;span&gt;&lt;span&gt;    a_tx_type,        \* what A is currently sending: &amp;quot;data&amp;quot; | &amp;quot;ack&amp;quot; | &amp;quot;nack&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    b_state,          \* current state of node B
&lt;&#x2F;span&gt;&lt;span&gt;    b_tx_type,        \* what B is currently sending: &amp;quot;data&amp;quot; | &amp;quot;ack&amp;quot; | &amp;quot;nack&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;    \* Carrier sense (shared physical observation, not ownership)
&lt;&#x2F;span&gt;&lt;span&gt;    carrier_sense,    \* &amp;quot;busy&amp;quot; | &amp;quot;idle&amp;quot; -- is the channel currently in use
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;    \* Transmission tracking
&lt;&#x2F;span&gt;&lt;span&gt;    a_window,         \* how many frames A advertised it will send this turn
&lt;&#x2F;span&gt;&lt;span&gt;    b_window,         \* how many frames B advertised it will send this turn
&lt;&#x2F;span&gt;&lt;span&gt;    a_sent,           \* how many frames A has sent this turn (resets each window)
&lt;&#x2F;span&gt;&lt;span&gt;    b_sent,           \* how many frames B has sent this turn (resets each window)
&lt;&#x2F;span&gt;&lt;span&gt;    a_total_sent,     \* cumulative frames A has ever sent (never resets)
&lt;&#x2F;span&gt;&lt;span&gt;    b_total_sent,     \* cumulative frames B has ever sent (never resets)
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;    \* Delivery tracking
&lt;&#x2F;span&gt;&lt;span&gt;    a_delivered,      \* highest seq number A has delivered to its application
&lt;&#x2F;span&gt;&lt;span&gt;    b_delivered,      \* highest seq number B has delivered to its application
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;    \* Peer-acknowledged counts (A&amp;#39;s&#x2F;B&amp;#39;s local knowledge of remote delivery)
&lt;&#x2F;span&gt;&lt;span&gt;    \* updated when an ACK is received; A is done when a_peer_acked = MAX_SENT
&lt;&#x2F;span&gt;&lt;span&gt;    a_peer_acked,     \* how many of A&amp;#39;s frames B has confirmed via ACK
&lt;&#x2F;span&gt;&lt;span&gt;    b_peer_acked,     \* how many of B&amp;#39;s frames A has confirmed via ACK
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;    \* Channel-request flags (piggybacked on ACK frames)
&lt;&#x2F;span&gt;&lt;span&gt;    \* set in an ACK when the sender still has data, cleared when it starts transmitting
&lt;&#x2F;span&gt;&lt;span&gt;    \* if b_wants_channel is set, A must yield; if a_wants_channel is set, B must yield
&lt;&#x2F;span&gt;&lt;span&gt;    a_wants_channel,
&lt;&#x2F;span&gt;&lt;span&gt;    b_wants_channel,
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;    \* Transmission history flags (set once, never cleared)
&lt;&#x2F;span&gt;&lt;span&gt;    \* used for fairness: both sides must get at least one window
&lt;&#x2F;span&gt;&lt;span&gt;    a_ever_transmitted,
&lt;&#x2F;span&gt;&lt;span&gt;    b_ever_transmitted,
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;    \* ACK-due flags (local knowledge only -- no peer state required)
&lt;&#x2F;span&gt;&lt;span&gt;    \* set when the peer finishes a window (you owe them an ACK)
&lt;&#x2F;span&gt;&lt;span&gt;    \* cleared when you send your ACK (DeliverAndAck&#x2F;Nack)
&lt;&#x2F;span&gt;&lt;span&gt;    \* prevents starting a new window before discharging your ACK obligation
&lt;&#x2F;span&gt;&lt;span&gt;    a_ack_due,        \* A owes B an ACK (set by BFinishTransmit)
&lt;&#x2F;span&gt;&lt;span&gt;    b_ack_due,        \* B owes A an ACK (set by AFinishTransmit)
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;    \* Pending retransmit flags
&lt;&#x2F;span&gt;&lt;span&gt;    \* set when a NACK is received, cleared when retransmit completes
&lt;&#x2F;span&gt;&lt;span&gt;    a_nack_pending,
&lt;&#x2F;span&gt;&lt;span&gt;    b_nack_pending
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;vars == &amp;lt;&amp;lt;a_state, a_tx_type, b_state, b_tx_type, carrier_sense,
&lt;&#x2F;span&gt;&lt;span&gt;          a_window, b_window, a_sent, b_sent, a_total_sent, b_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;          a_delivered, b_delivered,
&lt;&#x2F;span&gt;&lt;span&gt;          a_peer_acked, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;          a_wants_channel, b_wants_channel,
&lt;&#x2F;span&gt;&lt;span&gt;          a_ever_transmitted, b_ever_transmitted,
&lt;&#x2F;span&gt;&lt;span&gt;          a_ack_due, b_ack_due,
&lt;&#x2F;span&gt;&lt;span&gt;          a_nack_pending, b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;(* TypeInvariant                                                          *)
&lt;&#x2F;span&gt;&lt;span&gt;(*                                                                        *)
&lt;&#x2F;span&gt;&lt;span&gt;(* Every variable must stay within its legal range at all times.         *)
&lt;&#x2F;span&gt;&lt;span&gt;(* TLC will check this at every reachable state.                         *)
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;TypeInvariant ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_state        \in NodeStates
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_tx_type      \in NodeTxType
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_state        \in NodeStates
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_tx_type      \in NodeTxType
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense  \in {&amp;quot;busy&amp;quot;, &amp;quot;idle&amp;quot;}
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_window       \in 0..MAX_WINDOW
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_window       \in 0..MAX_WINDOW
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_sent         \in 0..MAX_WINDOW
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_sent         \in 0..MAX_WINDOW
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_total_sent   \in 0..MAX_SENT
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_total_sent   \in 0..MAX_SENT
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_delivered    \in 0..MAX_SEQ
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_delivered    \in 0..MAX_SEQ
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_peer_acked   \in 0..MAX_SENT
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_peer_acked   \in 0..MAX_SENT
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_wants_channel     \in BOOLEAN
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_wants_channel     \in BOOLEAN
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_ever_transmitted  \in BOOLEAN
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ever_transmitted  \in BOOLEAN
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_ack_due           \in BOOLEAN
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ack_due       \in BOOLEAN
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_nack_pending  \in BOOLEAN
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_nack_pending  \in BOOLEAN
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;(* Your turn -- write Init, actions, Next, Spec, and properties below    *)
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Init ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_state = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_tx_type = &amp;quot;data&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_state = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_tx_type = &amp;quot;data&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_window = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_window = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_sent = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_sent = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_total_sent = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_total_sent = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_delivered = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_delivered = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_peer_acked = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_peer_acked = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_wants_channel = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_wants_channel = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_ever_transmitted = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ever_transmitted = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_ack_due = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ack_due = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_nack_pending = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_nack_pending = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;AStartTransmit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_state = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_peer_acked = a_total_sent            \* B has ACKed A&amp;#39;s previous window
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ ~a_ack_due                             \* A has discharged its own ACK obligation to B
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ ~b_wants_channel                       \* yield if B called dibs in its last ACK
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ \E w \in 1..MAX_WINDOW :               \* TLC explores all window sizes
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ a_total_sent + w &amp;lt;= MAX_SENT       \* don&amp;#39;t promise frames you can&amp;#39;t send
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ a_window&amp;#39; = w
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_state&amp;#39;        = &amp;quot;transmitting&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_tx_type&amp;#39;      = &amp;quot;data&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_sent&amp;#39;         = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense&amp;#39;  = &amp;quot;busy&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_state&amp;#39;        = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_wants_channel&amp;#39;    = FALSE             \* A is using the channel, clear its own flag
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_ever_transmitted&amp;#39; = TRUE             \* record that A has transmitted at least once
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;b_tx_type, b_window, b_sent, b_total_sent, a_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_delivered, a_peer_acked, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   b_wants_channel, b_ever_transmitted,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ack_due, b_ack_due, a_nack_pending, b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;ASendFrame ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_state = &amp;quot;transmitting&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_sent &amp;lt; a_window
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_total_sent &amp;lt; MAX_SENT
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_sent&amp;#39;       = a_sent + 1
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_total_sent&amp;#39; = a_total_sent + 1
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_state, a_tx_type, b_state, b_tx_type, carrier_sense,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_window, b_window, b_sent, b_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_delivered, a_peer_acked, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_wants_channel, b_wants_channel,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ever_transmitted, b_ever_transmitted,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ack_due, b_ack_due, a_nack_pending, b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;AFinishTransmit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_state = &amp;quot;transmitting&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_sent = a_window
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_state&amp;#39;   = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense&amp;#39; = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ack_due&amp;#39; = TRUE                   \* B now owes A an ACK
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_tx_type, b_state, b_tx_type,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_window, b_window, a_sent, b_sent, a_total_sent, b_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_delivered, a_peer_acked, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_wants_channel, b_wants_channel,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ever_transmitted, b_ever_transmitted,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ack_due, a_nack_pending, b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;BStartTransmit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_state = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_peer_acked = b_total_sent            \* A has ACKed B&amp;#39;s previous window
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ ~b_ack_due                             \* B has discharged its own ACK obligation to A
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ ~a_wants_channel                       \* yield if A called dibs in its last ACK
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ \E w \in 1..MAX_WINDOW :               \* TLC explores all window sizes
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ b_total_sent + w &amp;lt;= MAX_SENT       \* don&amp;#39;t promise frames you can&amp;#39;t send
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ b_window&amp;#39; = w
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_state&amp;#39;        = &amp;quot;transmitting&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_tx_type&amp;#39;      = &amp;quot;data&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_sent&amp;#39;         = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense&amp;#39;  = &amp;quot;busy&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_state&amp;#39;        = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_wants_channel&amp;#39;    = FALSE             \* B is using the channel, clear its own flag
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ever_transmitted&amp;#39; = TRUE             \* record that B has transmitted at least once
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_tx_type, a_window, a_sent, a_total_sent, b_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_delivered, a_peer_acked, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_wants_channel, a_ever_transmitted,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ack_due, b_ack_due, a_nack_pending, b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;BSendFrame ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_state = &amp;quot;transmitting&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_sent &amp;lt; b_window
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_total_sent &amp;lt; MAX_SENT
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_sent&amp;#39;       = b_sent + 1
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_total_sent&amp;#39; = b_total_sent + 1
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_state, a_tx_type, b_state, b_tx_type, carrier_sense,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_window, b_window, a_sent, a_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_delivered, a_peer_acked, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_wants_channel, b_wants_channel,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ever_transmitted, b_ever_transmitted,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ack_due, b_ack_due, a_nack_pending, b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;BFinishTransmit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_state = &amp;quot;transmitting&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_sent = b_window
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_state&amp;#39;       = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense&amp;#39; = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_ack_due&amp;#39;     = TRUE                   \* A now owes B an ACK
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_state, a_tx_type, b_tx_type,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_window, b_window, a_sent, b_sent, a_total_sent, b_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_delivered, a_peer_acked, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_wants_channel, b_wants_channel,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ever_transmitted, b_ever_transmitted,
&lt;&#x2F;span&gt;&lt;span&gt;                   b_ack_due, a_nack_pending, b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;\* In the perfect-connection model, frames are delivered instantly and
&lt;&#x2F;span&gt;&lt;span&gt;\* completely when the transmitter finishes.  ACK actions run after
&lt;&#x2F;span&gt;&lt;span&gt;\* AFinishTransmit &#x2F; BFinishTransmit while the carrier is idle.
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;\* B received all of A&amp;#39;s frames cleanly -- sends a cumulative ACK.
&lt;&#x2F;span&gt;&lt;span&gt;\* Piggybacks b_wants_channel if B still has frames to send.
&lt;&#x2F;span&gt;&lt;span&gt;BDeliverAndAck ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ack_due = TRUE              \* B owes A an ACK (set when A finished its window)
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_delivered &amp;lt; a_total_sent
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_delivered&amp;#39;     = a_total_sent
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_peer_acked&amp;#39;    = a_total_sent
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_wants_channel&amp;#39; = (b_total_sent &amp;lt; MAX_SENT)
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ack_due&amp;#39;       = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_state, a_tx_type, b_state, b_tx_type, carrier_sense,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_window, b_window, a_sent, b_sent, a_total_sent, b_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_wants_channel, a_ever_transmitted, b_ever_transmitted, a_ack_due,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_nack_pending, b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;\* B received only a prefix of A&amp;#39;s frames due to loss -- sends a NACK.
&lt;&#x2F;span&gt;&lt;span&gt;\* \E n \in ... means TLC explores every possible loss scenario:
&lt;&#x2F;span&gt;&lt;span&gt;\*   n = b_delivered         =&amp;gt; all frames in this window were lost
&lt;&#x2F;span&gt;&lt;span&gt;\*   n = a_total_sent - 1    =&amp;gt; only the last frame was lost
&lt;&#x2F;span&gt;&lt;span&gt;\* A learns via a_peer_acked how far delivery got, and sets a_nack_pending
&lt;&#x2F;span&gt;&lt;span&gt;\* so it knows to retransmit from that point.
&lt;&#x2F;span&gt;&lt;span&gt;BDeliverAndNack ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ack_due = TRUE              \* B owes A an ACK (set when A finished its window)
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_delivered &amp;lt; a_total_sent
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ \E n \in b_delivered..(a_total_sent - 1) :  \* some frames lost; n is last good frame
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ b_delivered&amp;#39;  = n
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ a_peer_acked&amp;#39; = n
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_nack_pending&amp;#39;  = TRUE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_wants_channel&amp;#39; = (b_total_sent &amp;lt; MAX_SENT)
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ack_due&amp;#39;       = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_state, a_tx_type, b_state, b_tx_type, carrier_sense,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_window, b_window, a_sent, b_sent, a_total_sent, b_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_wants_channel, a_ever_transmitted, b_ever_transmitted, a_ack_due,
&lt;&#x2F;span&gt;&lt;span&gt;                   b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;\* A received all of B&amp;#39;s frames cleanly -- sends a cumulative ACK.
&lt;&#x2F;span&gt;&lt;span&gt;\* Piggybacks a_wants_channel if A still has frames to send.
&lt;&#x2F;span&gt;&lt;span&gt;ADeliverAndAck ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_ack_due = TRUE              \* A owes B an ACK (set when B finished its window)
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_delivered &amp;lt; b_total_sent
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_delivered&amp;#39;     = b_total_sent
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_peer_acked&amp;#39;    = b_total_sent
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_wants_channel&amp;#39; = (a_total_sent &amp;lt; MAX_SENT)
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_ack_due&amp;#39;       = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_state, a_tx_type, b_state, b_tx_type, carrier_sense,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_window, b_window, a_sent, b_sent, a_total_sent, b_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   b_delivered, a_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   b_wants_channel, a_ever_transmitted, b_ever_transmitted, b_ack_due,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_nack_pending, b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;\* A received only a prefix of B&amp;#39;s frames due to loss -- sends a NACK.
&lt;&#x2F;span&gt;&lt;span&gt;ADeliverAndNack ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_ack_due = TRUE              \* A owes B an ACK (set when B finished its window)
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_delivered &amp;lt; b_total_sent
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ \E n \in a_delivered..(b_total_sent - 1) :  \* some frames lost; n is last good frame
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ a_delivered&amp;#39;  = n
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ b_peer_acked&amp;#39; = n
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_nack_pending&amp;#39;  = TRUE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_wants_channel&amp;#39; = (a_total_sent &amp;lt; MAX_SENT)
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_ack_due&amp;#39;       = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_state, a_tx_type, b_state, b_tx_type, carrier_sense,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_window, b_window, a_sent, b_sent, a_total_sent, b_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   b_delivered, a_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   b_wants_channel, a_ever_transmitted, b_ever_transmitted, b_ack_due,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;\* A got a NACK: roll back a_total_sent to the last confirmed frame so that
&lt;&#x2F;span&gt;&lt;span&gt;\* the next AStartTransmit re-sends from that point.  Clears the pending flag.
&lt;&#x2F;span&gt;&lt;span&gt;\* Retransmit is only possible when the carrier is idle (A waits its turn).
&lt;&#x2F;span&gt;&lt;span&gt;ARetransmit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_nack_pending = TRUE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_state = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_total_sent&amp;#39; = a_peer_acked  \* rewind to last ACK&amp;#39;d frame
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_nack_pending&amp;#39; = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_state, a_tx_type, b_state, b_tx_type, carrier_sense,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_window, b_window, a_sent, b_sent, b_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_delivered, a_peer_acked, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_wants_channel, b_wants_channel,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ever_transmitted, b_ever_transmitted,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ack_due, b_ack_due, b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;BRetransmit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_nack_pending = TRUE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_state = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_total_sent&amp;#39; = b_peer_acked
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_nack_pending&amp;#39; = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_state, a_tx_type, b_state, b_tx_type, carrier_sense,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_window, b_window, a_sent, b_sent, a_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_delivered, a_peer_acked, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_wants_channel, b_wants_channel,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ever_transmitted, b_ever_transmitted,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_ack_due, b_ack_due, a_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;ADone == a_peer_acked = MAX_SENT
&lt;&#x2F;span&gt;&lt;span&gt;BDone == b_peer_acked = MAX_SENT
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Done == ADone &#x2F;\ BDone &#x2F;\ UNCHANGED vars
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;Next ==
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; AStartTransmit
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; ASendFrame
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; AFinishTransmit
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; BStartTransmit
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; BSendFrame
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; BFinishTransmit
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; BDeliverAndAck
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; BDeliverAndNack
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; ADeliverAndAck
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; ADeliverAndNack
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; ARetransmit
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; BRetransmit
&lt;&#x2F;span&gt;&lt;span&gt;    \&#x2F; Done
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;\* WF_vars(Next): every continuously-enabled action eventually fires (no deadlock)
&lt;&#x2F;span&gt;&lt;span&gt;\* SF_vars(BDeliverAndAck&#x2F;ADeliverAndAck): even with repeated loss, a clean
&lt;&#x2F;span&gt;&lt;span&gt;\* delivery must eventually happen -- models a lossy-but-not-permanently-broken
&lt;&#x2F;span&gt;&lt;span&gt;\* channel rather than one that can drop every frame forever.
&lt;&#x2F;span&gt;&lt;span&gt;Spec == Init &#x2F;\ [][Next]_vars &#x2F;\ WF_vars(Next)
&lt;&#x2F;span&gt;&lt;span&gt;           &#x2F;\ SF_vars(BDeliverAndAck)
&lt;&#x2F;span&gt;&lt;span&gt;           &#x2F;\ SF_vars(ADeliverAndAck)
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;(* Safety properties                                                      *)
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;NoSimultaneousTransmit ==
&lt;&#x2F;span&gt;&lt;span&gt;    ~(a_state = &amp;quot;transmitting&amp;quot; &#x2F;\ b_state = &amp;quot;transmitting&amp;quot;)
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;(* Liveness properties                                                    *)
&lt;&#x2F;span&gt;&lt;span&gt;(**************************************************************************)
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;BothEventuallyDone ==
&lt;&#x2F;span&gt;&lt;span&gt;    &amp;lt;&amp;gt;(ADone &#x2F;\ BDone)
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;BothGetATurn ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ (ADone =&amp;gt; b_ever_transmitted)
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ (BDone =&amp;gt; a_ever_transmitted)
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;=============================================================================
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;The first thing you might notice is that this spec is... long compared to the previous one.&lt;&#x2F;p&gt;
&lt;p&gt;This is a more complex example though. It largely avoids &amp;quot;magic&amp;quot; variables that allow each side to have an immediate and infallible view of the state of the other. After all, this is a network protocol, we can&#x27;t just directly access the memory of the other side (at least not reliably, and not without delay). Unfortunately, with TLA+, this is a thing &lt;em&gt;you&lt;&#x2F;em&gt; have to enforce.&lt;&#x2F;p&gt;
&lt;p&gt;To give a specific example of this, one might have a variable called &amp;quot;channel_owner&amp;quot; that has a value in &lt;code&gt;{&amp;quot;side_a&amp;quot;, &amp;quot;side_b&amp;quot;, &amp;quot;idle&amp;quot;}&lt;&#x2F;code&gt;. And this would certainly make our Action definitions easier to write. But that would be cheating. Instead we have internal states, and only access &lt;code&gt;side_b&lt;&#x2F;code&gt; state in cases where a message would have legitimately passed from one side of the network to the other. But, since we have carrier sense, we do have one shared state called &lt;code&gt;carrier_sense&lt;&#x2F;code&gt;, which is either &amp;quot;busy&amp;quot; or &amp;quot;idle&amp;quot;. And I think you can see how this simulates how carrier sense would actually be used by the real protocol. It&#x27;s a physical observation, not shared state, so it isn&#x27;t cheating that both sides get access to it.&lt;&#x2F;p&gt;
&lt;p&gt;For the most part, this is just a longer more complex example with more of what we&#x27;ve already seen, but there is some syntax we haven&#x27;t seen before, so let&#x27;s dig into some of this. For example:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;tla+&quot; style=&quot;background-color:#2b303b;color:#c0c5ce;&quot; class=&quot;language-tla+ &quot;&gt;&lt;code class=&quot;language-tla+&quot; data-lang=&quot;tla+&quot;&gt;&lt;span&gt;AStartTransmit ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_state = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ \E w \in 1..MAX_WINDOW :               \* TLC explores all window sizes
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ a_total_sent + w &amp;lt;= MAX_SENT       \* don&amp;#39;t promise frames you can&amp;#39;t send
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ a_window&amp;#39; = w
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_state&amp;#39;      = &amp;quot;transmitting&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_tx_type&amp;#39;    = &amp;quot;data&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_sent&amp;#39;       = 0
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense&amp;#39; = &amp;quot;busy&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_state&amp;#39;      = &amp;quot;listening&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;b_tx_type, b_window, b_sent, b_total_sent, a_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_delivered, a_peer_acked, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_nack_pending, b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Specifically &lt;code&gt;\E w \in 1..MAX_WINDOW&lt;&#x2F;code&gt;, which is an existential quantifier. This means that TLC will explore &lt;strong&gt;ALL&lt;&#x2F;strong&gt; window sizes in &lt;strong&gt;every&lt;&#x2F;strong&gt; case where this action fires! Every single window size that can legally occur (based on configuration and type invariants) will occur.&lt;&#x2F;p&gt;
&lt;p&gt;And, in this case, we&#x27;ve embedded some badness here in the form of NACK messages instead of ACK messages:&lt;&#x2F;p&gt;
&lt;pre style=&quot;background-color:#2b303b;color:#c0c5ce;&quot;&gt;&lt;code&gt;&lt;span&gt;\* B received only a prefix of A&amp;#39;s frames due to loss -- sends a NACK.
&lt;&#x2F;span&gt;&lt;span&gt;\* \E n \in ... means TLC explores every possible loss scenario:
&lt;&#x2F;span&gt;&lt;span&gt;\*   n = b_delivered         =&amp;gt; all frames in this window were lost
&lt;&#x2F;span&gt;&lt;span&gt;\*   n = a_total_sent - 1    =&amp;gt; only the last frame was lost
&lt;&#x2F;span&gt;&lt;span&gt;\* A learns via a_peer_acked how far delivery got, and sets a_nack_pending
&lt;&#x2F;span&gt;&lt;span&gt;\* so it knows to retransmit from that point.
&lt;&#x2F;span&gt;&lt;span&gt;BDeliverAndNack ==
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ carrier_sense = &amp;quot;idle&amp;quot;
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ack_due = TRUE              \* B owes A an ACK (set when A finished its window)
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_delivered &amp;lt; a_total_sent
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ \E n \in b_delivered..(a_total_sent - 1) :  \* some frames lost; n is last good frame
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ b_delivered&amp;#39;  = n
&lt;&#x2F;span&gt;&lt;span&gt;        &#x2F;\ a_peer_acked&amp;#39; = n
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ a_nack_pending&amp;#39;  = TRUE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_wants_channel&amp;#39; = (b_total_sent &amp;lt; MAX_SENT)
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ b_ack_due&amp;#39;       = FALSE
&lt;&#x2F;span&gt;&lt;span&gt;    &#x2F;\ UNCHANGED &amp;lt;&amp;lt;a_state, a_tx_type, b_state, b_tx_type, carrier_sense,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_window, b_window, a_sent, b_sent, a_total_sent, b_total_sent,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_delivered, b_peer_acked,
&lt;&#x2F;span&gt;&lt;span&gt;                   a_wants_channel, a_ever_transmitted, b_ever_transmitted, a_ack_due,
&lt;&#x2F;span&gt;&lt;span&gt;                   b_nack_pending&amp;gt;&amp;gt;
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;And once again, our friend the existential quantifier shows up, because this action explores &lt;strong&gt;EVERY&lt;&#x2F;strong&gt; possible delivery cutoff point.&lt;&#x2F;p&gt;
&lt;p&gt;Additionally, our &lt;code&gt;Done&lt;&#x2F;code&gt; action gets a little more complex, since it&#x27;s really a meta-state made up of two other states:&lt;&#x2F;p&gt;
&lt;pre style=&quot;background-color:#2b303b;color:#c0c5ce;&quot;&gt;&lt;code&gt;&lt;span&gt;Done == ADone &#x2F;\ BDone &#x2F;\ UNCHANGED vars
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;And we have this:&lt;&#x2F;p&gt;
&lt;pre data-lang=&quot;tla+&quot; style=&quot;background-color:#2b303b;color:#c0c5ce;&quot; class=&quot;language-tla+ &quot;&gt;&lt;code class=&quot;language-tla+&quot; data-lang=&quot;tla+&quot;&gt;&lt;span&gt;Spec == Init &#x2F;\ [][Next]_vars &#x2F;\ WF_vars(Next)
&lt;&#x2F;span&gt;&lt;span&gt;           &#x2F;\ SF_vars(BDeliverAndAck)
&lt;&#x2F;span&gt;&lt;span&gt;           &#x2F;\ SF_vars(ADeliverAndAck)
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;Remember how TLC explores every possible combination of states exhaustively? Consider a behavior where the system simply refuses to send frames indefinitely. We need to be fair (enough) to our system to give it a &lt;em&gt;chance&lt;&#x2F;em&gt; to execute despite all the chaos we are throwing at it, so we have a concept of fairness:&lt;&#x2F;p&gt;
&lt;p&gt;&lt;code&gt;WF_vars(Next)&lt;&#x2F;code&gt; is a &lt;strong&gt;weak fairness&lt;&#x2F;strong&gt; formula: if any action in &lt;code&gt;Next&lt;&#x2F;code&gt; is continuously enabled, it must eventually fire. That prevents TLC from exploring behaviors where the system just sits idle forever despite being able to make progress.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;code&gt;SF_vars(BDeliverAndAck)&lt;&#x2F;code&gt; and &lt;code&gt;SF_vars(ADeliverAndAck)&lt;&#x2F;code&gt; are &lt;strong&gt;strong fairness&lt;&#x2F;strong&gt; formulas: even if these actions are only intermittently enabled (i.e. repeatedly becoming available but not staying enabled continuously), TLC must still find behaviors where they &lt;em&gt;eventually&lt;&#x2F;em&gt; fire. This is a stronger guarantee than weak fairness provides, and it matters here because an ACK action can be temporarily blocked (the carrier might be busy, the other side might be transmitting) before becoming available again. Without strong fairness, TLC is allowed to explore behaviors where the ACK never gets sent simply because it kept getting interrupted at inconvenient moments. Strong fairness rules that out: if &lt;code&gt;BDeliverAndAck&lt;&#x2F;code&gt; is enabled often enough, it will eventually happen.&lt;&#x2F;p&gt;
&lt;p&gt;In practical terms: this is how you tell TLC that your liveness properties (&lt;code&gt;BothEventuallyDone&lt;&#x2F;code&gt;) must hold in a real execution, not just in a perfectly cooperative one.&lt;&#x2F;p&gt;
&lt;p&gt;This is about as complex a spec as I&#x27;ve worked with so far, and while it isn&#x27;t as complex as some examples, the state space is absolutely massive. For example, with this configuration:&lt;&#x2F;p&gt;
&lt;pre style=&quot;background-color:#2b303b;color:#c0c5ce;&quot;&gt;&lt;code&gt;&lt;span&gt;SPECIFICATION Spec
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;CONSTANTS
&lt;&#x2F;span&gt;&lt;span&gt;    MAX_WINDOW = 10
&lt;&#x2F;span&gt;&lt;span&gt;    MAX_SEQ    = 20
&lt;&#x2F;span&gt;&lt;span&gt;    MAX_SENT   = 20
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;INVARIANTS
&lt;&#x2F;span&gt;&lt;span&gt;    TypeInvariant
&lt;&#x2F;span&gt;&lt;span&gt;    NoSimultaneousTransmit
&lt;&#x2F;span&gt;&lt;span&gt;    BothGetATurn
&lt;&#x2F;span&gt;&lt;span&gt;
&lt;&#x2F;span&gt;&lt;span&gt;PROPERTIES
&lt;&#x2F;span&gt;&lt;span&gt;    BothEventuallyDone
&lt;&#x2F;span&gt;&lt;&#x2F;code&gt;&lt;&#x2F;pre&gt;
&lt;p&gt;TLC explored 10,745,891 states, 4,131,391 of which were distinct, with a maximum depth of 59 actions. And in all 10 million of them, our liveness and safety properties held.&lt;&#x2F;p&gt;
&lt;p&gt;If we can faithfully reproduce this spec (there may yet be magical variables we can&#x27;t actually implement in a real protocol), we can be assured that our system should behave correctly in all legal circumstances.&lt;&#x2F;p&gt;
&lt;p&gt;Ten million valid states is a lot of confidence to walk away with. So what do you actually do with it?&lt;&#x2F;p&gt;
&lt;h1 id=&quot;the-art-and-science-of-using-specs&quot;&gt;The Art And Science of Using Specs&lt;&#x2F;h1&gt;
&lt;p&gt;The spec is a precise description of your system that you can translate directly into implementation decisions.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Preconditions become runtime guards.&lt;&#x2F;strong&gt; Every action in a TLA+ spec has a precondition (the lines without primes that must hold before the action can fire). In &lt;code&gt;Debit&lt;&#x2F;code&gt;, that was &lt;code&gt;a_balance &amp;gt;= 10&lt;&#x2F;code&gt;. In &lt;code&gt;AStartTransmit&lt;&#x2F;code&gt;, it was &lt;code&gt;a_state = &amp;quot;listening&amp;quot;&lt;&#x2F;code&gt; and &lt;code&gt;carrier_sense = &amp;quot;idle&amp;quot;&lt;&#x2F;code&gt;. These map directly to assertions or guard clauses in your implementation. If your code does a debit without checking the balance first, you already know from the spec that this is wrong; the model only allowed that action when the guard held.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Invariants become unit tests.&lt;&#x2F;strong&gt; &lt;code&gt;MoneyConserved&lt;&#x2F;code&gt; and &lt;code&gt;NoSimultaneousTransmit&lt;&#x2F;code&gt; are properties TLC checked in every state. You can write the exact same checks as assertions in your test suite. The difference is that TLC proved them over every reachable state; your unit tests will only check the cases you thought to write. But having the invariant spelled out precisely in the spec means you know exactly what the test &lt;strong&gt;should&lt;&#x2F;strong&gt; assert.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;Error traces become regression tests.&lt;&#x2F;strong&gt; When TLC finds a violation, it prints the exact sequence of states that led to it, like the Debit-then-Crash sequence that broke &lt;code&gt;MoneyConserved&lt;&#x2F;code&gt; in our bank example. That trace is a regression test handed to you for free. Encode it: set up the initial state, execute those actions in that order, and assert the invariant holds not only in your spec, but in the real codebase.&lt;&#x2F;p&gt;
&lt;p&gt;&lt;strong&gt;The state machine is your integration test scaffold.&lt;&#x2F;strong&gt; The &lt;code&gt;Next&lt;&#x2F;code&gt; action defines every legal transition in the system. For the radio protocol, that meant &lt;code&gt;AStartTransmit&lt;&#x2F;code&gt;, &lt;code&gt;ASendFrame&lt;&#x2F;code&gt;, &lt;code&gt;BDeliverAndAck&lt;&#x2F;code&gt;, &lt;code&gt;BDeliverAndNack&lt;&#x2F;code&gt;, &lt;code&gt;ARetransmit&lt;&#x2F;code&gt;, &lt;code&gt;BRetransmit&lt;&#x2F;code&gt;, and so on. A good integration test suite should exercise every one of these transitions, and the spec tells you exactly what preconditions need to be true to get there and what the system should look like afterward.&lt;&#x2F;p&gt;
&lt;p&gt;Not every bit of logic will end up in your spec; scope is a deliberate choice. In the radio protocol, we abstracted away byte-level framing, timing jitter, and the exact contents of frames. The spec becomes a record of what you decided not to formally verify, which is exactly where traditional unit tests should focus.&lt;&#x2F;p&gt;
&lt;p&gt;In my experience, the skill in using AI tools effectively is communicating as specifically as possible how you want the AI to go about generating your code. Perhaps this is why they are so efficient when it comes to translating between one language to another. And a TLA spec is both incredibly precise and well scoped. It is a great way to tell a model: here are the details that are crucial to the safe behavior of this codebase (and nothing more).&lt;&#x2F;p&gt;
&lt;p&gt;That said, there are a few honest caveats. TLC can only check what you told it to check: if your invariants are incomplete or your model doesn&#x27;t faithfully represent the real system, you get false confidence. A contract that doesn&#x27;t say the right things is as useless as no contract at all. State space explosion is also a real concern; large constants can make TLC runs take hours or become infeasible entirely, which is part of why scoping your spec carefully matters. And finally, specs go stale. For simpler systems, once the implementation is built and validated, the spec may have served its purpose. But for more complex ones, keeping it around is a useful reminder of the logic problems you already solved, and a handy thing to point at when someone else breaks those contractual obligations (even and especially when that someone is &lt;em&gt;you&lt;&#x2F;em&gt;).&lt;&#x2F;p&gt;
&lt;h1 id=&quot;summary&quot;&gt;Summary&lt;&#x2F;h1&gt;
&lt;p&gt;TLA+ tends to get overlooked because you can&#x27;t directly compile a spec into running code. There is no &amp;quot;generate implementation&amp;quot; button. You write the spec, run TLC, fix the violations, and then you still have to go write the actual system. To someone under pressure to ship, that can feel like doing the work twice.&lt;&#x2F;p&gt;
&lt;p&gt;But I think that framing misses what the spec actually gives you. By the time TLC reports no errors across however many millions of states, you have already solved the hard problem: you know what your system is supposed to do, under what conditions each action is legal, what invariants must hold at all times, and which failure modes your design actually survives. The implementation becomes a translation exercise rather than a discovery process. And the spec leaves behind artifacts you can use: preconditions that become guard clauses, invariants that become assertions, error traces that become regression tests.&lt;&#x2F;p&gt;
&lt;p&gt;The Roomba design process works fine for a lot of problems. But for a distributed system or a stateful protocol, the state space is large enough that you genuinely cannot test your way to confidence without first understanding the shape of the problem. TLA+ is a tool for developing that understanding. And perhaps a TLA spec is also a great way to share that understanding with the AI model that is going to be writing your code anyway.&lt;&#x2F;p&gt;
&lt;p&gt;Going back to the questions I raised at the start: should we all be doing this? Do the old greybeards have a point? Did we leave a useful thing behind? Is this even still relevant? I can only answer for me: When I&#x27;m thinking about my next project, the likelihood of my reaching in my tool bag for TLA+ is directly proportional to how terrified I am of troubleshooting surprise deadlocks or safety bugs in the production code.&lt;&#x2F;p&gt;
&lt;br&gt;
&lt;br&gt;
&lt;br&gt;
&lt;div class=&quot;footnote-definition&quot; id=&quot;2&quot;&gt;&lt;sup class=&quot;footnote-definition-label&quot;&gt;1&lt;&#x2F;sup&gt;
&lt;p&gt;&lt;a href=&quot;https:&#x2F;&#x2F;www.anthropic.com&#x2F;engineering&#x2F;april-23-postmortem&quot;&gt;Anthropic April 23 Postmortem&lt;&#x2F;a&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
&lt;div class=&quot;footnote-definition&quot; id=&quot;1&quot;&gt;&lt;sup class=&quot;footnote-definition-label&quot;&gt;2&lt;&#x2F;sup&gt;
&lt;p&gt;Channel Activity Detection. It&#x27;s not full carrier sense like WiFi&#x27;s CSMA&#x2F;CA, it basically tells you if a LoRa pre-amble was detected recently and can be used in a similar way. It should work most of the time, but you might occasionally step on a transmission if you get very unlucky with timing. &lt;a href=&quot;https:&#x2F;&#x2F;www.semtech.com&#x2F;uploads&#x2F;technology&#x2F;LoRa&#x2F;cad-ensuring-lora-packets.pdf&quot;&gt;Semtech CAD paper&lt;&#x2F;a&gt;&lt;&#x2F;p&gt;
&lt;&#x2F;div&gt;
</content>
        
    </entry>
</feed>
